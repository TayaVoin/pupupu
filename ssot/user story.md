надо исправить, чтобы работало вычисление теплокарты на базе метода обратных взвешенных расстояний (IDW) по нескольким критериям: RSRP, RSRQ, RSSI, Altitude (для каждого EARFCN отдельно);
чтобы теплокарта сохранялась изображениями в отдельную папку в build 
исправить отображение центра на карте. к примеру, когда начинают поставить данные, карта должна переместиться так, чтобы точка местоположения была в центре окна с картой, а оно отображается в центре верхней части окна, а должна быть именно в центре окна


сами задания какие были изначально, но что-то было изменено по личной нужде (к примеру подгружается каждая десятая точка из бд)
еще до этого было задание с сокетами (ZMQ), это также должно обрабатывать и отправляться ответные сообще (в конце основных заданий прикреплю содержание этого)

X. Android Cellinfo.
Реализовать получение данных и передачу на backend-сервер след. данные о смартфоне:

При помощи класса Telephony получаем информацию о сетях мобильной связи:

CellInfoLte: CellIdentityLte, CellSignalStrengthLte;

CellIdentityLte: Band, CellIdentity, EARFCN, MCC, MNC, PCI, TAC;

CellSignalStrengthLte: ASU Level, CQI, RSRP, RSRQ, RSSI, RSSNR, Timing Advance;

CellInfoGsm: CellIdentityGSM, CellSignalStrengthGsm;

CellIdentityGSM: CellIdentity, BSIC, ARFCN, LAC, MCC, MNC, PSC;

CellSignalStrengthGsm: Dbm, RSSI, Timing Advance;

CellInfoNr: CellwIdentityNr, CellSignalStrengthNr

CellIdentityNr: Band, NCI, PCI, Nrargcn, TAC, MCC, MNC;

CellSignalStrengthNr: SS-RSRP, SS-RSRQ, SS-SINR, Timing Advance;

Данные о местоположении смартфона (см. практику №6):

Latitude;

Longitude;

Altitude;

Current Time;

Accuracy - точность вычисления местоположения;

ОПЦИОНАЛЬНО Информацию о сетевом трафике смартфона:

Информация об общем количестве переданных данных;

Информация о ТОП приложений (входящих в 2-сигма по потреблению трафика), потребляющих интернет-трафик смартфона.

11. Android background service.
Реализовать в приложении Android сервис работы в фоновом режиме. Пример здесь.

Перенести функцию определения местоположения в Service;

Перенести функцию получения данных о сети в Service;

Перенести отправку посредством сокетов в Service.

Реализовать отображение параметров мощности радиосигнала (CellSignalStrength) на графиках по мере получения данных с мобильного устройства.

Для графиков использовать библиотеку ImPlot.

Обновить git-репозиторий.

12. Драйв-тест.
Провести драйв-тест в городской местности (например, г. Новосибирск);

Пройтись по городу с телефоном, сохраняя статистику в json-файле (это у вас уже должно работать);

Json-файл должен соответствовать формату из Практики №10;

Собрать максимально возможное количество данных (за 1 неделю).

13. Работа с базой данных (PostgreSQL) из С/С++
Доработать код backend-сервера для взаимодействия с базой данных PSQl.

Создать собственную базу данных (пример);

Создать таблицу на основе данных, которые вы собираете с Android-приложения (Практика X). Пример создания таблицы по ссылке выше;

При каждом получении данных с Android-приложения, добавить данные в таблицу (пример на языке СИ);

В GUI-потоке приложения отображать только текущее значение (пока НЕ загружаем из базы данных);

Графики уровня сигнала (RSRP, RSSI, SINR) также отбражаем, кладя значения в массив по мере поступления данных.

При получении данных о НЕСКОЛЬКИХ сотах, отображаем графики для каждого PCI. Например, пришли данные о двух сотах со значениями PCI_1 = 7 (RSRP = -100 [dBm]), PCI_2 = 13 (RSRP = -89 [dBm]), на графиках должно параллельно отобразиться 2 значения (разными цветами) по оси Y в одном значении оси `X;

В легенде графика добавить значения для всех PCI.

Обновить Github-репозиторий. Обновить README.md, описать возможности вашего приложения.

14. Отрисовка OpenStreetMap карты
Примеры всех этапов можно найти в лекциях:

Про наименования тайлов - здесь;

Получаем тайлы из OSM-тайл-сервера, используя код С/C++ - здесь;

Работа с изображениями, преобразуем .png в пиксели и накладываем на implot - здесь.

Пример получения и отображения одного тайла: здесь.

Добработать backend-сервер для работы с OpenStreetMap картами:

Реализовать пересчет текущих координат в номера тайлов;

Получить .png-файл при помощи curl с сервера тайлов;

Преобразовать изображение .png в пиксельную карту (RGBa массив);

Отобразить карту на графике при помощи ImPlot в отдельном окне.

Необходимо заргужать несколько тайлов (зависиот от текущего размера окна ImGui):

Например, если размер окна равен 512x512, то вы должны загрузить и отобразить 4 тайла (256x256 [pix] каждый);

Количество загружаемых тайлов должно динамически меняться в зависимости от изменения размера окна.

Адаптировать вашу карту под изменения местоположения центра экрана и zoom’а:

Т.е. имитировать перемещение карты “на лету”.

Все, уже загруженные, тайлы сохранять в директориях build/zoom/x/y.png:

Заметьте, zoom это директория, x - это тоже директория, y - это конечный .png-файл.

Если тайл, который вы хотите отобразить уже был загружен, выводим на экран (из директории)

Иначе, загружаем через curl.

Провести рефакторинг кода, разделить разные по смыслу сущности (функции) на разные файлы:

Например, функции по работе с curl сложить в файл curl_func.h/.cpp;

и т.д. ДЛЯ ВСЕХ функций (сокеты, БД, ImGui, и т.д.).

Со звездочкой. Реализовать асинхронную загрузку изображений во время отправки запросов curl.

Обновить github-репозиторий, дописать README.md.

В REAMDE.md должны быть описаны пункты: установка зависимостей, компиляция, запуск ПО, описание архитектуры ПО.

15. Генерация тепловой карты (Heatmap)
Примеры каждого этапа формирования тепловой карты можно нати в лекции.

Реализовать генерацию тепловой карты на базе метода обратных взвешенных расстояний (IDW).

Выполнил вычисление по нескольким критериям: RSRP, RSRQ, RSSI, Altitude (для каждого EARFCN отдельно);

Ограничивать радиус вычисления интерполяции (учет экспериментальных значений) в пределах 10 - 40 метров до вычисляемой точки;

В ImGui-интерфейсе добавить выбор критерия, по которому будет отображаться тепловая карта.

Выполнить вычисление:

Либо одной большой картинкой для всех точек;

Либо для каждого тайла в отдельности.

Рекомендация выполнять вычисления в отдельном потоке.

Все вычисленные картинки тепловой карты сохранять в директориях build/zoom/x/y.png (если вычисляете для каждого тайла), било в /build (если одна общая картинка);

Обновить github-репозиторий, доработать README.md.

8. Работа с сокетами (ZMQ). Передача данных от Android к PC
На базе примера реализовать передачу данных внутри приложения Android (ваш предыдущий проект);

Создать новое Activity, в котором будет реализованы функции клиента и сервера;

Реализовать начало передачу данных между сервером и клиентом при нажатии на кнопку (создать новую кнопку);

Новый функционал должен находится в отдельной (от основной) ветке (создаем новую ветку (branch));

Наладить передачу данных от Вашего приложения (Android) на приложение-сервер на Вашем компьютере (или VPS, если имеется):

Сервер (компьютер) - запустить серверную часть на базе языка Python + ZMQ;

Android - выступает в роли клиента (на базе ZMQ), подключается к серверу;

Ваш компьютер и телефон (Android) должны находится в одной сети (Wifi, USB, Internet (если есть белый ip-адрес));

В роли данных со стороны Android-приложения выступает строка "Hello from Andorid!";

Данные со стороны сервера - "Hello from Server!";

Серверная часть ДОЛЖНА сохранить (в файл) каждый блок данных, пришедший со смартфона, и считать количество полученных пакетов.

Серверная часть также должны иметь метод\функцию по выводу на экран (консоль) всех сохраненных данных;

Git:

Android - Сделать c merge request с новым функционалом;

Server - добавить новый коммит, обновить удаленный репозиторий.